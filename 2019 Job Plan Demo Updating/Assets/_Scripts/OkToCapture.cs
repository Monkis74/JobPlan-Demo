using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// givenb to each page to identify it as being able to be screencaptured to pdf.

public class OkToCapture : MonoBehaviour {
    public int pageNum; // sets the order of the pages to capture. set in inspector.

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
